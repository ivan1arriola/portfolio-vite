import { Container } from "react-bootstrap";

const Footer = () => {
    return (
        <footer className="footer">
            <Container>
                <span className="text-muted">Ivan Arriola - Portfolio</span>
            </Container>
        </footer>
    );
};

export default Footer;